pipeline {
    agent {
        node {
            label 't058-runner'
        }
    }

    stages {
        stage('Tag and Push to Docker Hub') {
            steps {
                script {
                    def remoteServer = '44.203.225.123'
                    def remoteUser = 'ubuntu'
                    def remoteDockerImage = 'my-nginx'
                    def imageName = "sravani111/nginx-webapp:${env.BUILD_NUMBER}"
                    def latestImageName = "sravani111/nginx-webapp:latest"

                    withCredentials([
                        usernamePassword(credentialsId: 't058-docckerhub', usernameVariable: 'DOCKERHUB_USERNAME', passwordVariable: 'DOCKERHUB_PASSWORD')
                    ]) {
                        sshagent(credentials: ['ssh-key-creds']) {
                            sh """
                                ssh -o StrictHostKeyChecking=no -l ${remoteUser} ${remoteServer} 'docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD &&
                                docker tag ${remoteDockerImage} ${imageName} &&
                                docker tag ${remoteDockerImage} ${latestImageName} &&
                                docker push ${imageName} &&
                                docker push ${latestImageName} &&
                                docker logout'
                            """
                        }
                    }
                }
            }
        }
    }
}

